import { styled } from "../stitches.config";

const Button = styled("button", {
  all: "unset",
  background: "$crimson8",
  color: "$mauve1",
  padding: "18px 24px",
  borderRadius: "22px",
  fontSize: "$3",
  cursor: "pointer",
});

export default Button;
